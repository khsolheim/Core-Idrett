---
phase: 09-translation-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/features/achievements/presentation/achievement_admin_screen.dart
  - app/lib/features/achievements/presentation/achievements_screen.dart
  - app/lib/features/achievements/presentation/widgets/achievement_cards.dart
  - app/lib/features/statistics/presentation/widgets/player_profile_achievements_section.dart
  - app/lib/features/teams/presentation/team_detail_screen.dart
  - app/lib/features/teams/presentation/widgets/quick_links_widget.dart
  - app/lib/data/models/tournament_enums.dart
  - app/lib/features/mini_activities/presentation/widgets/match_result_sheet.dart
  - app/lib/features/chat/presentation/widgets/conversation_list_panel.dart
  - app/lib/features/points/presentation/widgets/points_toggle_settings.dart
  - app/lib/features/profile/presentation/profile_screen.dart
autonomous: true

must_haves:
  truths:
    - "All UI labels display in Norwegian with zero English words visible to users"
    - "All 'Achievements' references replaced with 'Prestasjoner' throughout the UI"
    - "Tournament enum displayNames use Norwegian terms instead of English"
    - "Chat title, role badges, and settings use Norwegian text"
  artifacts:
    - path: "app/lib/features/achievements/presentation/achievement_admin_screen.dart"
      provides: "Norwegian achievement admin titles"
      contains: "Prestasjoner"
    - path: "app/lib/data/models/tournament_enums.dart"
      provides: "Norwegian tournament status and match status displayNames"
      contains: "Trekning"
    - path: "app/lib/features/profile/presentation/profile_screen.dart"
      provides: "Norwegian role badge label"
      contains: "Administrator"
  key_links:
    - from: "app/lib/data/models/tournament_enums.dart"
      to: "tournament UI screens"
      via: "displayName getter"
      pattern: "\\.displayName"
    - from: "achievement screens"
      to: "team_detail_screen.dart"
      via: "navigation and labels"
      pattern: "Prestasjoner"
---

<objective>
Translate all remaining English user-facing strings to Norwegian across the frontend codebase.

Purpose: Complete the Norwegian-only UI requirement (I18N-01, I18N-02, I18N-03) by replacing the remaining ~5% of English strings.
Output: All user-facing text in app/lib/ displays Norwegian.
</objective>

<execution_context>
@/Users/karsten/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karsten/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-translation-completion/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate "Achievements" to "Prestasjoner" across all UI files</name>
  <files>
    app/lib/features/achievements/presentation/achievement_admin_screen.dart
    app/lib/features/achievements/presentation/achievements_screen.dart
    app/lib/features/achievements/presentation/widgets/achievement_cards.dart
    app/lib/features/statistics/presentation/widgets/player_profile_achievements_section.dart
    app/lib/features/teams/presentation/team_detail_screen.dart
    app/lib/features/teams/presentation/widgets/quick_links_widget.dart
  </files>
  <action>
Replace all user-facing "Achievement(s)" strings with Norwegian equivalents. The word "Prestasjoner" (achievements/accomplishments) is the standard Norwegian translation.

Specific replacements:

1. `achievement_admin_screen.dart`:
   - Line 32: `'Achievements Admin'` -> `'Prestasjoner'` (loading state AppBar)
   - Line 41: `'Administrer Achievements'` -> `'Administrer prestasjoner'`
   - Line 46: `'Ny achievement'` -> `'Ny prestasjon'`
   - Line 59 (EmptyState): `'Ingen egendefinerte achievements'` -> `'Ingen egendefinerte prestasjoner'`
   - Line 166: `'Slett achievement?'` -> `'Slett prestasjon?'`

2. `achievements_screen.dart`:
   - Line 36: `'Achievements'` -> `'Prestasjoner'`
   - Line 94 (EmptyState): `'Ingen achievements enna'` -> `'Ingen prestasjoner enna'`
   - Line 95: `'Delta i aktiviteter for a tjene achievements'` -> `'Delta i aktiviteter for a tjene prestasjoner'`
   - Line 192: `'Alle'` - already Norwegian, leave as-is
   - Line 276: `'Ingen achievements pa laget enna'` -> `'Ingen prestasjoner pa laget enna'`

3. `achievement_cards.dart`:
   - Line 149: `def?.name ?? 'Achievement'` -> `def?.name ?? 'Prestasjon'`
   - Line 212: `def?.name ?? 'Achievement'` -> `def?.name ?? 'Prestasjon'`

4. `player_profile_achievements_section.dart`:
   - Line 32: `'Achievements'` -> `'Prestasjoner'`
   - Line 52 (EmptyState): `'Ingen achievements'` -> `'Ingen prestasjoner'`
   - Line 53: `'Ingen achievements a vise enna'` -> `'Ingen prestasjoner a vise enna'`

5. `team_detail_screen.dart`:
   - Line 196: `'Achievements'` -> `'Prestasjoner'`
   - Line 197: `'Se badges og milepaeler'` -> `'Se merker og milepaeler'` (translate "badges" too)
   - Line 207: `'Administrer achievements'` -> `'Administrer prestasjoner'`
   - Line 208: `'Opprett og rediger achievements'` -> `'Opprett og rediger prestasjoner'`

6. `quick_links_widget.dart`:
   - Line 33: `label: 'Achievements'` -> `label: 'Prestasjoner'`

Do NOT rename file names, class names, route names, or variable names — those are code identifiers and stay in English per project convention. Only change string literals visible to users.
  </action>
  <verify>
Run: `cd "/Users/karsten/NextCore/Core - Idrett/app" && grep -rn "'.*[Aa]chievement" lib/features/ lib/data/ --include="*.dart" | grep -v "import\|class\|Provider\|Repository\|Screen\|Widget\|Section\|Cards\|fromJson\|parseList\|provider\|Provider\|achievementD\|achievementU\|ref\.\|watch\|read\|invalidate\|router\|pushNamed\|GoRoute\|path:\|name:"` should return zero lines of user-facing achievement strings.

Also run: `cd "/Users/karsten/NextCore/Core - Idrett/app" && flutter analyze` should show zero new errors.
  </verify>
  <done>All instances of "Achievement(s)" in user-facing Text widgets, labels, EmptyState titles/subtitles, and dialog titles replaced with "Prestasjon(er)". Zero English "achievement" strings visible to users. flutter analyze passes.</done>
</task>

<task type="auto">
  <name>Task 2: Translate remaining English terms (Seeding, Walkover, Chat, opt-out, Admin badge)</name>
  <files>
    app/lib/data/models/tournament_enums.dart
    app/lib/features/mini_activities/presentation/widgets/match_result_sheet.dart
    app/lib/features/chat/presentation/widgets/conversation_list_panel.dart
    app/lib/features/points/presentation/widgets/points_toggle_settings.dart
    app/lib/features/profile/presentation/profile_screen.dart
  </files>
  <action>
Translate the remaining scattered English terms to Norwegian:

1. `tournament_enums.dart`:
   - Line 104: TournamentStatus.seeding displayName `'Seeding'` -> `'Trekning'` (seeding/drawing in tournament context)
   - Line 276: MatchStatus.walkover displayName `'Walkover'` -> `'Walkover'` — NOTE: "Walkover" is actually used in Norwegian sports terminology as a loanword. Keep as-is. It is an accepted Norwegian sports term.

2. `match_result_sheet.dart`:
   - Line 155: `'Walk-over'` -> `'Walkover'` (normalize spelling to match the enum displayName — Norwegian uses "walkover" without hyphen)

3. `conversation_list_panel.dart`:
   - Line 59: `'Chat'` -> `'Meldinger'` (messages — more natural Norwegian for a messaging feature title)

4. `points_toggle_settings.dart`:
   - Line 38: `'Tillat opt-out'` -> `'Tillat reservasjon'` (opt-out = reservasjon in Norwegian)

5. `profile_screen.dart`:
   - Line 214: `label: 'Admin'` -> `label: 'Administrator'` (full Norwegian word)

Each change is a single-line string replacement. Do NOT modify any logic, only the string literal values.
  </action>
  <verify>
Run the following grep to confirm no remaining common English UI words in features:
`cd "/Users/karsten/NextCore/Core - Idrett/app" && grep -rn "Text('Chat')\|'Seeding'\|'Walk-over'\|'Tillat opt-out'\|label: 'Admin'" lib/ --include="*.dart"` should return zero results.

Run: `cd "/Users/karsten/NextCore/Core - Idrett/app" && flutter analyze` should show zero new errors.
  </verify>
  <done>All remaining scattered English terms translated: "Seeding" -> "Trekning", "Walk-over" normalized to "Walkover", "Chat" -> "Meldinger", "opt-out" -> "reservasjon", "Admin" badge -> "Administrator". flutter analyze passes.</done>
</task>

</tasks>

<verification>
1. Run `cd "/Users/karsten/NextCore/Core - Idrett/app" && flutter analyze` - should show zero new errors (only the 5 pre-existing warnings)
2. Grep for remaining English in user-facing strings: `grep -rn "Text('[A-Z][a-z]" lib/features/ --include="*.dart" | grep -iv "tekst\|tittel\|ingen\|alle\|velg\|opprett\|rediger\|slett\|avbryt\|lagre\|lukk\|legg\|godkjenn\|avvis\|meld\|registrer\|bekreft\|eksporter\|logg\|skriv\|vis\|fjern\|flytt\|endre\|klag\|betal\|generer\|del\|ny\|prøv\|send\|se\|fortsett\|nullstill\|deaktiver\|reaktiver\|vant\|stoppeklokke\|nedtelling\|bonus\|straff\|korreksjon\|individuell\|lag\|rangering\|walkover\|meldinger\|prestasjoner\|administrator\|trekning\|reservasjon"` — remaining matches should be only Norwegian words starting with capital letters
3. No file modifications outside `app/lib/` directory
</verification>

<success_criteria>
- All ~20 identified English user-facing strings replaced with Norwegian equivalents
- Zero "Achievement(s)" visible in UI (replaced with "Prestasjon(er)")
- Enum displayNames use Norwegian terms
- flutter analyze shows no new errors
- All changes are string-literal-only replacements (no logic changes)
</success_criteria>

<output>
After completion, create `.planning/phases/09-translation-completion/09-01-SUMMARY.md`
</output>
